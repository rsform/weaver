{
    "lexicon": 1,
    "id": "computer.nonbinary.embed.records",
    "description": "A set of records embedded in a blog post.",
    "defs": {
      "main": {
        "type": "object",
        "required": ["records"],
        "properties": {
          "images": {
            "type": "array",
            "items": { "type": "ref", "ref": "#computer.nonbinary.embed.defs#record" },
            "maxLength": 48
          }
        }
      },

    }
  }
